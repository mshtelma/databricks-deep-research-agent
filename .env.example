# Databricks Configuration
DATABRICKS_HOST=https://your-workspace.databricks.com

# Profile-based authentication (required for Lakebase OAuth)
# Profile must be configured in ~/.databrickscfg
DATABRICKS_CONFIG_PROFILE=your-profile-name

# Lakebase Configuration (OAuth-authenticated PostgreSQL on Databricks)
# Authentication flow:
#   1. Uses DATABRICKS_CONFIG_PROFILE to authenticate with Databricks
#   2. Looks up instance hostname via list_database_instances() API
#   3. Generates OAuth token via generate_database_credential() API
#   4. Connects with username from JWT 'sub' claim and token as password
LAKEBASE_INSTANCE_NAME=your-instance-name
LAKEBASE_DATABASE=deep_research
LAKEBASE_PORT=5432

# Fallback: Direct DATABASE_URL for local development
# To use local PostgreSQL instead of Lakebase:
#   1. Comment out LAKEBASE_INSTANCE_NAME above
#   2. Uncomment DATABASE_URL below
#   3. Run: docker compose up -d postgres
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/postgres

# Brave Search API
BRAVE_API_KEY=your-brave-api-key

# MLflow Tracking
MLFLOW_TRACKING_URI=databricks
MLFLOW_EXPERIMENT_NAME=/Workspace/Users/your-email@databricks.com/experiments/deep-research-agent

# Application Settings
APP_ENV=development
LOG_LEVEL=INFO
DEBUG=true

# Frontend API URL (for development)
VITE_API_URL=http://localhost:8000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
DEFAULT_TOKENS_PER_MINUTE=100000

# E2E Testing
# Base URL for Playwright E2E tests (used when running tests locally or in CI)
E2E_BASE_URL=http://localhost:8000
